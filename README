Доброго времени суток!
Приложение запускается при помощи Docker. Dockerfile и docker-compose.yml полностью сформированы.

Чтобы запустить приложение нужно выполнить две команды из корневой директории:

1) docker-compose build

2) docker-compose up

Затем выполните миграции "docker exec -it имя_вашего_контейнера python manage.py migrate".

Приложение готово к работе.

